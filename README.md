# Livestock Weight Prediction Model

This project is an experimental study on building a machine learning model to predict the weight of livestock (cattle, horses, sheep, and goats) based on their attribute data.
It implements a complete pipeline encompassing data preprocessing (specifically, One-Hot Encoding), data splitting, model training, and the comparison of prediction results.
